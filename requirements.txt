Flask>=3.1.3
Flask-Babel>=3.1.0
Flask-SQLAlchemy==3.0.5
SQLAlchemy>=2.0.0
Flask-Migrate==4.0.0
psycopg2-binary==2.9.10
flask-login==0.6.3
Werkzeug>=3.1.8
Jinja2>=3.1.5
python-slugify==8.0.1
Flask-WTF==1.2.2
wtforms==3.2.1
replit>=4.1.0
email_validator==2.2.0
python-dotenv>=1.2.2
flask-limiter==3.8.0
flask-seasurf==2.0.0
flask-talisman==1.1.0
flask-mail==0.10.0
flask-session==0.8.0
sentry-sdk==2.17.0
Flask-Caching==2.3.0
Flask-Compress>=1.14
redis==5.2.0
gunicorn>=22.0.0
gevent>=23.9.1
psycogreen>=1.0.1

# Email Service (Resend)
resend>=2.0.0

# Native Statement System (Phase 1 & 3)
# Note: scipy deferred - Wilson score can be calculated without it
# scipy will be added later with proper binary wheels
scikit-learn>=1.4.0
urllib3>=2.6.3
Pygments>=2.20.0
numpy>=1.24.0

# Background Tasks (Phase 3)
APScheduler==3.10.4

# Encryption for API keys (Phase 4)
# Upper bound <47 required: atproto (0.0.65) requires cryptography<47. Upgrade atproto when a version supporting >=47 is released.
cryptography>=46.0.4,<47
pandas>=2.0.0
dnspython>=2.6.1
markdown>=3.7
bleach>=6.1.0

# HTTP client (used across news, webhooks, Resend, third-party integrations)
requests>=2.33.0

# Session filesystem fallback (used when Redis is unavailable in non-prod)
cachelib>=0.9.0

# Slug/diacritic normalisation (used in app/models.py for URL-safe identifiers)
Unidecode>=1.3.0

# Analytics
posthog>=7.4.2

# CORS for API endpoints
flask-cors>=6.0.2

# Billing
stripe>=12.0.0

# News ingestion
feedparser>=6.0.12
googlenewsdecoder>=0.1.7

# LLMs (lazy-imported in generators and briefing; code uses the modern client API
# that exists in both openai 1.x and 2.x)
openai>=1.88.0
anthropic>=0.75.0

# Social Media Posting
atproto>=0.0.30
tweepy>=4.14.0

# Briefing System v2 - Ingestion
pypdf>=4.0.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.1
pytz>=2023.3

# TLS CA bundle (journey link checker, urllib clients on constrained hosts)
certifi>=2024.7.4

# Testing
pytest>=9.0.3
pytest-cov>=4.1.0

# Pin protobuf to compatible version (required by replit, googleapis packages)
protobuf>=4.21.8,<5.0.0
